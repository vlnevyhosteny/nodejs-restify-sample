{
  "name": "nodejsmonitor",
  "version": "1.0.0",
  "description": "Endpoint for monitoring another REST API endpoints.",
  "main": "index.js",
  "scripts": {
    "lint": "tslint --project .",
    "build": "node_modules/.bin/tsc",
    "test": "npm run build && NODE_ENV='test' node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha test/**/*.spec.js",
    "cover": "npm run test && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js",
    "start": "npm run build && node server.js",
    "inspect": "node --inspect server.js"
  },
  "author": "vnevyhosteny",
  "license": "MIT",
  "dependencies": {
    "curl-request": "^1.1.1",
    "node-libcurl": "^1.3.3",
    "restify": "^7.2.2"
  }
}
